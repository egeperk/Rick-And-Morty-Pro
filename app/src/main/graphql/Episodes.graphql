query Episode ($page: Int){
    episodes(page: $page) {
        results {
            id
            episode
            name
            air_date
        }info {
            count
            next
        }
    }
}